<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:Sailing"
             x:Class="Sailing.MainPage">
    <ContentPage.Content>
        <ScrollView>
            <StackLayout BackgroundColor="{StaticResource PrimaryColor}">
                <!-- Place new controls here -->
                <Label Text="Activities in your Area:" Style="{StaticResource PageHeader}"/>
                
                <StackLayout x:Name="Activity" BackgroundColor="#FFFFFF" Padding="20,0">
                    
                    <!--Click Event-->
                    <StackLayout.GestureRecognizers>
                        <TapGestureRecognizer x:Name="ActivityClicked" Tapped="ActivityClicked_Tapped"/>
                    </StackLayout.GestureRecognizers>
                    <!--Click Event-->
                    
                    <BoxView HeightRequest="10" BackgroundColor="{StaticResource TertiaryColor}"/>

                    <Image Source="RamseysExample.jpg" WidthRequest="300" HeightRequest="200"/>
                    <Label FontSize="5"/> <!--Blank Rule-->
                    <BoxView HeightRequest="1" BackgroundColor="Black"/>

                    <Label x:Name="ActivityTitle" Text="Ramsey's Steakhouse" FontSize="Title"  TextColor="Black" FontAttributes="Bold"/>
                    
                    <Label Margin="0,-12" FontSize="16" TextColor="Black">
                            <Label.FormattedText>
                                <FormattedString>
                                    <FormattedString.Spans>
                                        <Span Text="Type: " />
                                        <Span x:Name="ActivityType" Text="Restaurant" />
                                    </FormattedString.Spans>
                                </FormattedString>
                            </Label.FormattedText>
                    </Label>


                    <Label FontSize="16" TextColor="Black">
                        <Label.FormattedText>
                            <FormattedString>
                                <FormattedString.Spans>
                                    <Span Text="Location: "/>
                                    <Span x:Name="ActivityLocation" Text="Heerenveen, Eau Rouge 4"/>
                                </FormattedString.Spans>
                            </FormattedString>
                        </Label.FormattedText>
                    </Label>


                    <Label  Margin="0,-12" FontSize="16" TextColor="Black">
                        <Label.FormattedText>
                            <FormattedString>
                                <FormattedString.Spans>
                                    <Span Text="Enterance Fee: " />
                                    <Span x:Name="ActivityFee" Text="10"/>
                                    <Span Text="EUR"/>
                                </FormattedString.Spans>
                            </FormattedString>
                        </Label.FormattedText>
                    </Label>

                    <Label FontSize="12"/> <!--Blank Rule-->

                </StackLayout>

            </StackLayout>
        </ScrollView>
    </ContentPage.Content>
</ContentPage>
